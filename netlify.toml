[build]
  publish = "."
  functions = "netlify/functions"

# Lets the app call /api/live on Netlify too (same path as Vercel),
# so no code editing is needed when switching hosts.
[[redirects]]
  from = "/api/live"
  to = "/.netlify/functions/live"
  status = 200
